<div class="game-card">
	<div class="name">{{name}}</div>
	<div class="type">{{type}}</div>
	<div class="power">{{power}}</div>
	<div class="actions">
		{{#each action in actions}}
			<div class="action">
				{{action}}
			</div>
		{{/each}}
	</div>
</div>